import DecisionFlow from '../shared/DecisionFlow'

export default function DecisionFlowSlide({ title, fullscreen }) {
  return (
    <div className="max-w-[600px] mx-auto px-8 w-full">
      <DecisionFlow />
    </div>
  )
}
